# Ignore all warnings from all pods
inhibit_all_warnings!

platform :ios, '11.0'

target 'Sammys' do
  use_frameworks!
  pod 'PromiseKit', '~> 6.8'
  pod 'PromiseKit/Foundation'
  pod 'Firebase/Core'
  pod 'Firebase/Auth'
  pod 'Stripe'
end
